# Keep line endings stable regardless of the contributor's platform: this is a
# JavaScript package, so LF everywhere.
* text=auto eol=lf

# Binary assets must not be touched.
*.png binary
*.jpg binary
*.tgz binary
